[project]
name = "qq-chat-desensitizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
[project.scripts]
qq-cleaner = "chat_sanitizer.cli:main"

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
